###########################################
# Example environment variables for Vercel #
###########################################

# REQUIRED: Neon/PG pooled connection for runtime (Serverless functions)
# Use a pooled host with PgBouncer and a small connection limit
# e.g. ?sslmode=require&pgbouncer=true&connection_limit=1
DATABASE_URL="postgresql://USER:PASS@POOL_HOST/DB?sslmode=require&pgbouncer=true&connection_limit=1"

# REQUIRED: Direct connection for Prisma migrations (non-pooled)
# This URL is only used locally or in CI for migrations, not at runtime
DIRECT_URL="postgresql://USER:PASS@DIRECT_HOST/DB?sslmode=require"

# REQUIRED: JWT signing secret for auth tokens
JWT_SECRET="replace-with-strong-secret"

# OPTIONAL: Public site URL (used for redirects/cookies if needed)
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"

# Notes:
# - On Vercel, add these via: `vercel env add` and link them in vercel.json as @database-url, @direct-url, @jwt-secret
# - Keep secrets out of source control; this file is just an example.